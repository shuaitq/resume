name: Resume

on: [push]

jobs:
  Generate-PDF:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
